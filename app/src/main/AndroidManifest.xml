<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gachon.termproject.joker">
    <!-- 카메라, 외부 저장소 접근 권한 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Joker">
        <activity
            android:name=".activity.WriteReviewPostExpertListActivity"
            android:label="@string/title_activity_write_review_post"
            android:theme="@style/Theme.Joker.NoActionBar"
            android:noHistory="true" />
        <activity
            android:name=".activity.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.Signup00Activity" />
        <activity android:name=".activity.Signup01Activity" />
        <activity android:name=".activity.Signup02Activity" />
        <activity android:name=".activity.Signup03Activity" />
        <activity android:name=".activity.Signup04Activity" />
        <activity android:name=".activity.Signup05Activity" />
        <activity android:name=".activity.Signup06Activity" />
        <activity android:name=".activity.MainActivity" />
        <activity
            android:name=".activity.WritePostActivity"
            android:configChanges="screenSize|orientation">

            <!-- ^화면 회전 시 상태 및 데이터 초기화 방지^ -->
        </activity>
        <activity android:name=".activity.SeePostActivity" />
        <activity android:name=".activity.ChatActivity" />
        <activity android:name=".fragment.PortfolioMyInfo" />
        <activity android:name=".activity.FindPassword" />
    </application>

</manifest>